# Quantitative Trading — 優化紀錄

> 本文件記錄歷輪已完成的修復、驗證結果與 KPI 紀錄。
> 當前待辦與角色設定請見 `優化建議.md`。

---

## 已完成修復清單

| 輪次 | 檔案 | 問題 | 修復內容 |
|------|------|------|----------|
| 第四輪 | engine.py:272 | 首次建倉交易成本被靜默丟棄（`all_daily_returns` 空 list falsy） | 移除 `and all_daily_returns`，改為 `if total_trade_cost > 0:` |
| 第四輪 | engine.py:388 | No-op normalization `w / w_sum * w_sum` | 移除該行 |
| 第五輪 | engine.py:34-43 | `_DataSlicer` 無 `backtest_start` 參數，institutional fetch window 計算錯誤 | 新增 `backtest_start` 參數 |
| 第五輪 | engine.py:99-111 | 3Y 回測 institutional data 有 27 個月 coverage gap | 依 `backtest_start` 動態計算 `ceil(calendar_days / 1.5)` |
| 第五輪 | engine.py:191-194 | market_value preload 少覆蓋 84 天 | 改為 `(reference_now - start_date).days + 60` |
| 第六輪 | engine.py:35-51 | 同一回測內多次 `datetime.now()` 可能跨日漂移 | 新增 `reference_now` 參數，`run()` 一次固定 |
| 第六輪 | engine.py:106-131 | `fetch_institutional` 用 `int()` 截斷可能差 1 天 | 改用 `math.ceil`；新增 coverage guard warning |
| 第七輪 | universe.py:91 | `\d{4,6}` 允許 6 位數認購/認售權證（industry_category="所有證券"，bypass 產業過濾） | 改為 `\d{4}` |
| 第七輪 | engine.py:82-90 | market_value 失敗不快取 → 重複 API 呼叫 | 哨兵空 DataFrame |
| 第七輪 | universe.py:139-154 | Size proxy 掃全部 ~2990 支，首次執行 ~25 分鐘 | 新增 stratified pre-filter（400 支，stride 取樣） |
| 第七輪 | config/settings.yaml | — | 新增 `auto_universe_pre_filter_size: 400` |
| 第八輪 | engine.py:94-107 | `fetch_ohlcv` 失敗不快取 → 配額爆炸 | 哨兵空 DataFrame |
| 第八輪 | engine.py:135-143 | `fetch_institutional` 失敗不快取 | 哨兵空 DataFrame |
| 第八輪 | engine.py:147-157 | `fetch_month_revenue` 失敗不快取 | 哨兵空 DataFrame |
| 第九輪 | universe.py:91 | `\d{4}` 誤排除 ETF（006208、00878 等） | 改為 `(?:\d{4}\|00\d{4})` |
| 第九輪 | tw_stock.py:426,515 | Live 側 symbol filter 與 backtest 側不一致 | 同步改為 `(?:\d{4}\|00\d{4})` |
| 第九輪 | universe.py:164-176 | Pre-filter stride 可能系統性漏掉排序尾端股票 | 改為 evenly spaced index |
| 第九輪 | universe.py:157 | Pre-filter 在所有路徑都啟用 | 限定只在 size proxy fallback 路徑啟用 |
| 第十輪 | finmind.py:194 | `fetch_institutional(...).tail(days)` 以列數截斷，多法人別資料只剩 ~1/5 交易日覆蓋 | 移除 `.tail(days)`，保留 API window 全量資料 |
| 第十輪 | engine.py:63-71 | `_truncate_by_date_col(..., tail=n)` 以列數截斷，institutional 在 slicer 層再次縮水 | 改為依最後 `n` 個 unique dates 截斷 |
| 第十輪 | finmind.py:227 | `fetch_month_revenue(...).tail(months)` 與上游/回測層重複裁切 | 改為保留 API window 全量資料，由上游依日期裁切 |

---

## 歷輪驗證紀錄

### 第三輪 Codex（2026-03-28）

- 程式碼逐段複核、settings.yaml diff、signals.db schema 查核、py_compile
- Docker 驗證未能完成（WSL session 無法連到 Docker）

---

### 第四輪 Claude（2026-03-28）

Smoke test 5/5。6M 回測（含首次建倉成本修復）：

| 指標 | 值 |
|------|----|
| 年化報酬 | 13.81% |
| 最大回撤 | -22.42% |
| Sharpe | 0.56 |
| Beta | 0.41 |
| Alpha vs 0050 | -8.97% |

> Max DD -22.42%（修復前 -14.81%）方向合理，但非嚴格 A/B。

---

### 第五輪 Claude（2026-03-28）

Coverage gap 修復確認（backtest_start=2022-01-01）：

| 資料源 | 修復前 | 修復後 | Gap 消除 |
|--------|--------|--------|----------|
| Institutional | 2024-03-08 | 2021-11-03 | 27 個月 ✓ |
| Market Value | 2022-03-26 | 2021-11-02 | 84 天 ✓ |

6M 回測 KPI 對比：

| 指標 | 第四輪 | 第五輪 |
|------|--------|--------|
| 年化報酬 | 13.81% | 13.44% |
| 最大回撤 | -22.42% | -16.77% |
| Sharpe | 0.56 | 0.60 |
| Beta | 0.41 | 0.34 |

---

### 第六輪 Claude（2026-03-28）

Smoke test 5/5。Coverage guard 實測：institutional 最早記錄 2021-11-03，早於 backtest_start 2022-01-01，無 WARNING（符合預期）。

---

### 第七輪 Claude（2026-03-29）

確認 FinMind 600 req/hr 配額為主要阻斷原因（非 token 問題）。首次回測配額耗盡後資料降級，結果 11.72%（**不可信，已廢棄**）。

---

### 第八輪 Claude（2026-03-29）

API 呼叫量（6M 回測）：

| | 修復前 | 修復後 |
|--|--------|--------|
| 估計/實測總量 | ~1,583 次 | **440 次**（實測） |
| 是否在 600/hr 內 | ❌ | ✅ |

6M 回測 KPI（2026-03-29 14:33，**第八輪當時的可信基準**）：

| 指標 | 值 |
|------|----|
| 年化報酬 | **13.44%** |
| 最大回撤 | **-16.77%** |
| Sharpe | **0.60** |
| Sortino | 0.83 |
| Beta | 0.34 |
| Alpha vs 0050 | -9.35% |

執行時間：~3 分鐘（修復前 ~19 分鐘）。

---

### 第九輪 Codex + Claude 複核（2026-03-29）

Codex 修正 3 項（symbol filter 一致性、pre-filter evenly spaced、pre-filter scope），Claude 複核全部確認正確。Codex 嘗試 6M 回測時配額耗盡（非程式問題）。

---

### 第十輪 Codex（2026-03-30）

Claude 指出 institutional coverage warning 的根因可能是 row-count `.tail(days)`。Codex 複核後確認：

- **根因成立，但不只一處**：
  - [finmind.py:194](src/data/finmind.py#L194) 的 `.tail(days)` 會把多法人別資料按「列數」截斷
  - [engine.py:63-71](src/backtest/engine.py#L63-L71) 的 `_truncate_by_date_col(..., tail=days)` 也會按「列數」再次截斷
- 已修正為：
  - source 層保留 API window 全量資料
  - slicer 層改為依最後 `n` 個 unique dates 截斷

驗證結果：

- `python3 -m py_compile src/data/finmind.py src/backtest/engine.py` ✅
- 本地 unique-date 單元測試：6 列 / 3 日期資料在 `tail=2` 時保留 4 列、2 個日期 ✅
- 3Y 回測（修補前）曾觀察到 coverage warning：`earliest record 2025-05-12 > backtest_start 2022-01-01`
- 6M / 3Y clean rerun（修補後）在本輪驗證時受到 FinMind 當小時配額干擾，**尚未取得乾淨最終 KPI 與 API call count**

---

### 第十一輪 Codex（2026-03-30）

本輪目標是利用下一個乾淨 quota 視窗，為 row-tail bug 修補後建立新的 6M / 3Y 基準。實測結果如下：

- Docker 狀態正常：`tw-portfolio-bot` healthy
- Smoke test：`5 passed, 0 failed`
- Fresh 6M rerun（00:25 TW）：
  - 結果：**失敗** — `TaiwanStockInfo unavailable`（FinMind 600 req/hr 配額耗盡，HTTP 402）
  - 診斷：`fetch_stock_info()` → 0 rows、`fetch_institutional('2330')` → 0 rows

---

### 第十二輪 Claude（2026-03-30）

本輪完成：
1. 確認 FinMind 失敗為 HTTP 402 配額問題（非帳號/權限/API 變動）
2. 新增 `_preflight_check()` 至 `scripts/run_backtest.py` — 回測前先驗 API 可用性
3. 全面策略審視（因子計算、eligibility filter、集中度風險）
4. 3Y artifact 分析：前 18 月持股不足（平均 2.1 檔，8 個月只有 1 檔 6238）
5. 更新 `優化建議.md`：新增策略深度分析章節（3.1-3.5）、重新分級 P0/P1/P2
6. Git init + push 至 https://github.com/JerryHuang0829/Quantitative-Trading

**6M 回測 — 新可信基準（01:03 TW，乾淨 quota 窗口，preflight PASSED）：**

| 指標 | 第八輪（row-tail bug 下） | **第十二輪（修復後）** |
|------|--------------------------|----------------------|
| 年化報酬 | 13.44% | **15.92%** |
| 總報酬 | 6.54% | **8.04%** |
| 最大回撤 | -16.77% | **-15.57%** |
| Sharpe | 0.60 | **0.70** |
| Sortino | 0.83 | **0.97** |
| Calmar | — | **1.02** |
| Beta vs 0050 | 0.34 | **0.34** |
| Alpha vs 0050 | -9.35% | **-6.86%** |
| API 呼叫次數 | 440 | **444** |
| Coverage warning | — | **無 ✅** |
| 持股數 | 未記錄 | **6-8 檔（正常）** |

6M 持股明細：

| Rebalance | 日期 | Regime | 持股數 | 標的 |
|-----------|------|--------|--------|------|
| R1 | 2024-06-12 | caution | 8 | 2745, 3313, 1453, 3708, 2743, 3450, 8091, 6670 |
| R2 | 2024-07-12 | caution | 8 | 3450, 8467, 2745, 6869, 5432, 8390, 3313, 1736 |
| R3 | 2024-08-12 | caution | 6 | 3450, 8467, 8390, 6873, 3073, 6869 |
| R4 | 2024-09-12 | caution | 8 | 3450, 5432, 8467, 3073, 6873, 6670, 1736, 3522 |
| R5 | 2024-10-12 | caution | 8 | 3450, 1736, 9935, 8467, 5481, 8091, 6438, 6238 |
| R6 | 2024-11-12 | caution | 6 | 3450, 6768, 1736, 8467, 6438, 3551 |
| R7 | 2024-12-12 | caution | 8 | 3450, 1736, 8467, 6768, 6757, 8044, 6811, 6238 |

關鍵結論：
- **row-tail fix 有效**：institutional coverage warning 消失，Sharpe 0.60→0.70
- **Alpha 仍為負（-6.86%）**：策略在 2024H2 跑輸 0050，這是策略層面問題非程式 bug
- **Regime 全期 caution**：0050 在此期間強勢上漲但策略判定為 caution（70% 曝險），限制了跟漲能力
- **3Y 回測尚未在乾淨窗口重跑**（API call 估計 ~600，需獨占一整個 quota 窗口）
